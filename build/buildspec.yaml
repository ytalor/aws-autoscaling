version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
  pre_build:
    commands:
      -  echo "pre build"
  build:
    commands:
      - echo "Running Test"
artifacts:
  secondary-artifacts:
    AppArtifacts:
      discard-paths: yes
      files:
        - code/index.html
        - deploy/*
    InfraArtifacts:
      discard-paths: yes
      files:
        - infra/global-resources.yaml